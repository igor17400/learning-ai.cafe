## What

<!-- One sentence. It becomes the ledger entry, so say what the reader gets: "Add a worked example to the softmax page". -->

## Where

- [ ] tutorial: `tutorials/<slug>`
- [ ] pages touched:
- [ ] figures added:

## Checks

- [ ] `pnpm build` passes
- [ ] numbers in the text match the code that computes them
- [ ] figures have alt text and a caption that states the takeaway

<!-- Reviewers: set a `type:*` label (page, fix, figure, worked-example, review, translation) and, if the default is wrong, a `score:N` label before merging. -->
